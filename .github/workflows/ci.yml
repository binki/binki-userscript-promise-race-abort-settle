name: CI
on:
  push:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: action/checkout@v4
    - uses: actions/setup-node@v4
      with:
        cache: 'npm'
    - run: npm install
    - run: npm run build --if-present
    - run: npm test
